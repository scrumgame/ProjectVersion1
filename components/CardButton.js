import React, { Component } from 'react';

export default class CardButton extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <div>
        <button>Prio</button>
      </div>
    );
  }
}
